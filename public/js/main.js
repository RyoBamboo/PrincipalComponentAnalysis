$(function(){
    /**
     * ステップナビゲーション周りの処理
     */
    // 現在のURLを取得
        /*
    var url = location.href;
    var query = url.split('#');
    var stepCount = query[1].split('/').length;
    console.log(stepCount);
    $('.btn-next').on('click', function() {
        // ステップナビゲーションのアニメーション
        $('.line').addClass('show');
    });
    */
});